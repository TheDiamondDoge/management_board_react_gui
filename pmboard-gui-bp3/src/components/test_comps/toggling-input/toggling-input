import React from 'react';
import PropTypes from 'prop-types';

export default class TogglingInput extends React.Component {
    constructor(props){
        console.log(props);
        super(props);
        this.state = {
            isEditing: false,
            defaultValue: ""
        }
    }
    render() {
        // const { type, name, isEditing, defaultValue, ...rest } = this.props;
        return (
            <>
            {
                isEditing
                    ? <input
                        {/*{...rest}*/}
                        // type={type}
                        // name={name}
                        // defaultValue={defaultValue}
                        />
                    : <div></div>

            }
            </>
        );
    }
}

TogglingInput.propTypes = {
    type: PropTypes.string,
    name: PropTypes.string,
    isEditing: PropTypes.bool,
    defaultValue: PropTypes.oneOfType([
        PropTypes.string,
        PropTypes.number,
        PropTypes.bool,
        PropTypes.instanceOf(Date),
    ]),
};